---
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";
import { RepartoProcessParticipantsView } from "@mano8/astro-reparto-m8/default-ui";
import "../scaffold/styles/reparto.css";

export function getStaticPaths() {
  return [{ params: { processId: "current" } }];
}

const processId = Astro.params.processId ?? "current";
const config = {
  apiBase: import.meta.env.PUBLIC_FA_REPARTO_API_BASE,
  apiPrefix: import.meta.env.PUBLIC_FA_REPARTO_API_PREFIX
};
---

<StarlightPage
  frontmatter={{
    title: "Reparto participants",
    description: "Create, edit, and delete reparto process participants (teacher roster selection) for the active process.",
    tableOfContents: false
  }}
>
  <RepartoProcessParticipantsView client:load config={config} processId={processId} />
</StarlightPage>